{
  "name": "nidaan-kiosk-server",
  "version": "1.0.0",
  "private": true,
  "description": "Express + PostgreSQL backend for the Nidaan patient intake kiosk",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "prisma:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:remove-ramesh": "node scripts/remove-ramesh.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "@prisma/client": "^6.16.0"
  },
  "devDependencies": {
    "prisma": "^6.16.0"
  }
}
